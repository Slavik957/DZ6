// Напишите программу, которая найдёт точку пересечения двух прямых, заданных уравнениями y = k1 * x + b1, y = k2 * x + b2; значения b1, k1, b2 и k2 
//задаются пользователем.


 double k1, b1, k2, b2;

        // Ввод коэффициентов прямых пользователем
        Console.WriteLine("Введите коэффициенты первой прямой:");
        Console.Write("k1 = ");
        k1 = double.Parse(Console.ReadLine());
        Console.Write("b1 = ");
        b1 = double.Parse(Console.ReadLine());

        Console.WriteLine("Введите коэффициенты второй прямой:");
        Console.Write("k2 = ");
        k2 = double.Parse(Console.ReadLine());
        Console.Write("b2 = ");
        b2 = double.Parse(Console.ReadLine());

        // Поиск точки пересечения
        double x = (b2 - b1) / (k1 - k2);
        double y = k1 * x + b1;

        // Вывод результатов
        Console.WriteLine($"Точка пересечения прямых: ({x}, {y})");


       
// Программа начинается с объявления переменных для коэффициентов прямых `k1`, `b1`, `k2` и `b2`. Далее пользователю предлагается ввести значения этих коэффициентов.

//Затем происходит расчет координаты `x` точки пересечения по формуле `(b2 - b1) / (k1 - k2)`, а затем расчет координаты `y` с использованием одного из уравнений прямых.

//Наконец, результат выводится на экран с использованием строки формата.